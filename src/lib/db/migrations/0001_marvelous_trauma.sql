CREATE TABLE `client` (
	`id` varchar(128) PRIMARY KEY NOT NULL,
	`cookies` boolean,
	`mobile` boolean,
	`os_name` text,
	`os_version` text,
	`cpu_arch` text,
	`engine_name` text,
	`engine_version` text,
	`browser_name` text,
	`browser_version` text,
	`lang` text,
	`sys_lang` text,
	`screen_print` text,
	`local_storage` boolean,
	`plugins` text,
	`vendor_flavor` text,
	`device_memory` int,
	`color_depth` int,
	`color_gamut` text,
	`hardware_concurrency` int,
	`hdr` boolean,
	`languages` text,
	`openDatabase` boolean,
	`os_cpu` int,
	`pdf_viewer` boolean,
	`platform` text,
	`res` text,
	`session_storage` text,
	`device_tz` text,
	`touch_points` int,
	`vendor` text,
	`video_card` text,
	`video_card_vendor` text,
	`fp_visitor_id` text);
--> statement-breakpoint
CREATE TABLE `ip` (
	`id` varchar(128) PRIMARY KEY NOT NULL,
	`ip` text,
	`city` text,
	`ip_ype` text,
	`eu` boolean,
	`postal` text,
	`region` text,
	`country` text,
	`tor` boolean,
	`vpn` boolean,
	`proxy` boolean,
	`hosting` boolean,
	`anonymous` boolean,
	`tz` text,
	`tz_dst` boolean,
	`tz_time` text,
	`ip_org` text,
	`ip_asn` text,
	`ip_isp` text,
	`ip_domain` text,
	`region_code` text,
	`calling_code` text);
